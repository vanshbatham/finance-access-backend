spring:
  application:
    name: finance-access-backend

  datasource:
    url: jdbc:mysql://localhost:3306/financedb?useSSL=false
    username: root
    password: Vansh@sql16 # update with your MySQL credentials
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
